#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <cmath>
#include <limits.h>
#include <map>
using namespace std;

int main() {
    char a; cin >> a;
    if(a >= 'A' && a <= 'Z') cout << 'A' << endl;
    else cout << 'a' << endl;
}